아래 내용을 새 Cowork 대화창에 복사해서 붙여넣으세요.

---

## 프로젝트 이어받기

나는 소노호텔앤리조트 GS팀 담당자야. 두 개 프로젝트를 운영 중이야:

1. **트렌드 리포트**: ~/Desktop/gs_daily_trend_news_public_temp/
2. **경쟁사 모니터링**: ~/Projects/sono-competitor-crawler/

이전 세션에서 샌드박스 디스크 부족으로 못 한 작업들이 있어. 아래 순서대로 진행해줘.

### 1단계: git push (긴급)
두 레포 모두 로컬에 커밋이 쌓여있어. push 먼저 해줘.

```bash
cd ~/Desktop/gs_daily_trend_news_public_temp && git add -A && git commit -m "GSN nav update: Booking Status 추가 + 담당자분석 세그필터" && git push origin main

cd ~/Projects/sono-competitor-crawler && git add -A && git commit -m "경쟁사 모니터링 GSN 네비게이터 추가" && git push origin main
```

충돌나면 enriched_notes.json 머지 충돌 먼저 해결하고 push해.

### 2단계: Revenue Meeting PDF 분석
- 경로: ~/Desktop/gs_daily_trend_news_public_temp/data/RM자료/
- PDF 100개+ (2023.12 ~ 최근)
- 최신 PDF 구조 분석해서 FCST 데이터 추출
- docs/fcst-admin.html에 "RM FCST" 열 추가 (기존FCST, 자동FCST 다음 열)
- AI 변동요인도 갱신

### 3단계: 당일분석 YoY
- scripts/build.py 수정 — 당일분석 테이블에 동기간 YoY 비교 추가

### 4단계: 주간활동일지 분석
- 파일: 주간활동일지 2026 (121710).xlsx (uploads 또는 data 폴더에 있을 수 있음)
- DATA 시트 분석 → 데일리 리포트에 요약 추가 + Action Plan 대시보드
- 추후 회원번호/명 기반 실적관리 확장 감안해서 설계

### 참고사항
- 온북 데이터: 27+43 = 투숙일 기준, 27+43-28-44 = 예약일 기준. 혼용 금지
- GS_CHANNELS = ['Inbound', 'OTA', 'G-OTA']
- build.py가 index.html, otb.html을 덮어쓰므로 이 파일들은 직접 수정하지 말고 build.py 템플릿 수정
- 더미 데이터 절대 금지, 실데이터만 사용

두 폴더 모두 마운트해줘: ~/Desktop/gs_daily_trend_news_public_temp 와 ~/Projects/sono-competitor-crawler
